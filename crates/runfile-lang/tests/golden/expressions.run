# Precedence, indexing, calls, and every literal kind

let arithmetic = 1 + 2 * 3 - -4
let compared = x < 2 || y >= 3 && !z
let chained = ARG.a ? ENV.B ? "fallback"
let indexed = split("1.2.3", ".")[1]
let called = concat("a", to_upper("b"))
let list = [
	"a",
	2,
	true,
]
