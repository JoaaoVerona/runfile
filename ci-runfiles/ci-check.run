# Non-mutating CI gate: format check + Clippy (deny warnings) + type check

.alias = "ci:check"

$ cargo fmt --all -- --check
$ cargo clippy --all-targets --all-features --locked -- -D warnings
$ cargo check --all-targets --all-features --locked
