# Package and (re)install the extension into VS Code, replacing any existing copy

run package
$ code --install-extension runfile-vscode.vsix --force
print("Installed — run \"Developer: Reload Window\" in VS Code to activate.")
